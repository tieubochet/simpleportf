{
  "name": "crypto-portfolio-tracker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp index.html metadata.json dist/ && sed -i 's|src=\"/index.tsx\"|src=\"/index.js\"|' dist/index.html && esbuild index.tsx --bundle --outfile=dist/index.js --loader:.tsx=tsx --platform=browser --jsx=automatic --external:react --external:react/ --external:react-dom/ --external:recharts --external:uuid --external:@google/genai"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
