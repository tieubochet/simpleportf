{
  "framework": null,
  "installCommand": "echo 'Skipping installation, as this project uses CDN imports.'",
  "buildCommand": "mkdir dist && cp index.html index.tsx metadata.json types.ts App.tsx dist/ && cp -r components hooks services utils dist/"
}
